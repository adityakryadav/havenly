# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
tsconfig.tsbuildinfo
*.tsbuildinfo

# Only pnpm is the supported package manager (see CONTRIBUTING.md / SETUP.md).
# Ignore npm/yarn lockfiles to prevent accidental dependency drift.
package-lock.json
yarn.lock

# Ignore app/images to prevent massive binary files from bloating the repo size
app/images/
